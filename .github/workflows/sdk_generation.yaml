name: Generate
permissions:
  checks: write
  contents: write
  pull-requests: write
  statuses: write
"on":
  workflow_dispatch:
    inputs:
      force:
        description: Force generation of SDKs
        type: boolean
        default: false
      set_version:
        description: optionally set a specific SDK version
        type: string
  schedule:
    - cron: 0 0 * * *
jobs:
  generate:
    uses: speakeasy-api/sdk-generation-action/.github/workflows/workflow-executor.yaml@a4daf3fab0660c258e9d0396bf578c8757add43e
    with:
      force: ${{ github.event.inputs.force }}
      mode: pr
      runs-on: windows-latest
      set_version: ${{ github.event.inputs.set_version }}
      speakeasy_version: latest
      pnpm_version: "9.11.0"
    secrets:
      github_access_token: ${{ secrets.GITHUB_TOKEN }}
      speakeasy_api_key: ${{ secrets.SPEAKEASY_API_KEY }}
